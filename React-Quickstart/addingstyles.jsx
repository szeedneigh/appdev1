{/* Adding Styles*/}

// In React, you specify a CSS class with className. It works the same way as the HTML class attribute:

<img ClassName="avatar" />

// Then you write the CSSS rules for it in a separate CSS file: see addingstyles.css